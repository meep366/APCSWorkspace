/**
 * 
 */
package com.meep;

import java.util.Map;
import java.util.TreeMap;

/**
 * @author jack
 *
 */
public class WordLinesThread implements Runnable {

	/**
	 * @param args
	 */
	String[] lines;
	
	public WordLinesThread(String[] data)
	{
		lines=data;
	}
	
	public void matchWordLines()
	{
		Map<String, Integer[]> lineNums = new TreeMap<String, Integer[]>();
		
		for(int i=0;i<lineNums.size();i++)
		{
			String regex="[\\s,!.;:{}\"]+";
			String[] lineWords=lines[i].replaceFirst("^"+regex, "").split(regex);
			
			for(int j=0;j<lineWords.length;j++)
			{
				
			}
		}
		
	}
	
	public void run()
	{
		matchWordLines();
	}
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
